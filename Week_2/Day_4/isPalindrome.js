// Write your code below
// Return true if this word is a palindrome. false if it is not. A palindrome is a word that is spelled the same backwards and forwards. Treat spaces and uppercase letters normally—so “Racecar” wouldn’t be a palindrome since “R” and “r” aren’t the same letter:

// const isPalindrome = (word) => {
//   let checker = word.length - 1;
//   for (let i = 0; i < word.length; i++) {
//     if (word[i] !== word[checker]) {
//       return false;
//     }
//     checker--;
//   }
//   return true;
// };

// console.log(isPalindrome("hello"));

// const isPalindrome = (word) => word.split("").reverse().join("") === word;

// console.log(isPalindrome("abba"));
